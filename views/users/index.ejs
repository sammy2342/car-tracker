<%- include('../partials/header') %> 

<h1>All user that sigend in </h1>
<% users.forEach( function(u) { %>
    <tr>
        <a href="/users/<%= u._id%> "><td class="all-user"><img src="<%= u.avatar %> " alt="avatar" referrerpolicy="no-referrer"><%= u.name %> </td></a>
    </tr>
    
<% })  %> 

<%- include('../partials/footer') %> 